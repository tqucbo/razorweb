@page "user/personaldata/"
@model PersonalDataModel
@{
    ViewData["Title"] = "Dữ liệu cá nhân";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Tài khoản này bao gồm các dữ liệu cá nhân mà bạn đã cung cấp cho chúng tôi.

            Trang này cho phép bạn tải xuống hoặc xoá toàn bộ dữ liệu.</p>
        <p>
            <strong>Xoá toàn bộ dữ liệu sẽ đi cùng với việc xoá tài khoản của bạn. Không thể khôi phục thao tác
                này.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-primary" type="submit">Tải xuống dữ liệu</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-secondary">Xoá dữ liệu</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
